/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['assets.aceternity.com', 'slotgames.partycasino.com', 'images.unsplash.com', 'img.freepik.com', 'scmedia.wv.betmgm.com'],
    },
};

export default nextConfig;
